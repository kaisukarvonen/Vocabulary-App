<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:layout_width="400dp"
            android:layout_height="wrap_content"
            android:text="Themes"
            android:id="@+id/themes_title"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="38dp"
            android:layout_marginBottom="10dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            />

        <ListView
            android:id="@+id/themes_listview"
            android:layout_width="match_parent"
            android:layout_height="380dp"
            android:layout_below="@+id/themes_title"/>

            <android.support.design.widget.TextInputLayout
                android:id="@+id/enter_theme_name"
                android:layout_width="230dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginLeft="36dp"
                app:hintEnabled="false"
                android:layout_below="@+id/themes_listview"
                >
                <EditText
                    android:id="@+id/new_theme_name"
                    android:hint="Enter new theme..."
                    android:layout_width="230dp"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.AppCompat.Body1"
                    />
            </android.support.design.widget.TextInputLayout>

            <ImageButton
                android:id="@+id/add_new_theme_btn"
                android:layout_width="27dp"
                android:layout_height="27dp"
                android:layout_toRightOf="@+id/enter_theme_name"
                android:layout_marginTop="15dp"
                android:background="@drawable/oval"
                android:clickable="true"
                android:onClick="saveNewTheme"
                android:src="@drawable/ic_add_240"
                app:backgroundTint="@color/colorPrimary"
                android:layout_below="@+id/themes_listview"/>



    </RelativeLayout>
</ScrollView>